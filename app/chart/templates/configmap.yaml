apiVersion: v1
kind: ConfigMap
metadata:
  name: cryptoledger-config
  namespace: crypto
  labels:
    app: cryptoledger
data:
{{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
